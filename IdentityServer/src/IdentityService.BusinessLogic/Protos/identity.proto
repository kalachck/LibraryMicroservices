syntax = "proto3";

option csharp_namespace = "IdentityService.BusinessLogic";

package identity;

service CheckUser{
	rpc Check (RequestEmail) returns (UserResponse);
}

message RequestEmail{
	string Email = 1;
}

message UserResponse{
	bool IsExists = 1;
}
