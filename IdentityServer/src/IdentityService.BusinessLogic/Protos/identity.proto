syntax = "proto3";

option csharp_namespace = "IdentityService.BusinessLogic";

package identity;

service CheckUser{
	rpc Check (RequestEmail) returns (ResponseMessage);
}

message RequestEmail{
	string Email = 1;
}

message ResponseMessage{
	bool IsExists = 1;
}
